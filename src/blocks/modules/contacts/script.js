import $ from "jquery";
import Parralax from "jquery-parallax.js"; // eslint-disable-line no-unused-vars

$("#parallax-contact").parallax({
    imageSrc: "../img/contacts/bg_contact.webp",
    naturalWidth: 1000,
});
